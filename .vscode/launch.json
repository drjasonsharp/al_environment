{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug NASM Program",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            "stopAtEntry": true,
            "preLaunchTask": "Link ASM"
        }
    ]
}